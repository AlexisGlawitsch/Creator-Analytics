// Generic data view component that utilizes a line graph from react-vis
// Pass in data via props to customize

import React from "react";

export default function DataView({ type, data }) {
  // By default, can filter by all time, year, month, week, and day, depending on
  // how much information is stored in the database

  return null;
}
